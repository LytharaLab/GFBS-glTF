package org.lytharalab.gfbs.gltf.api.animation;

/** OVERRIDE interpolates toward a sampled pose; ADDITIVE applies the clip's delta from the bind pose. */
public enum AnimationBlendMode {
    OVERRIDE,
    ADDITIVE
}
